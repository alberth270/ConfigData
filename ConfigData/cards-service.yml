---
number-card: 111122223333444
# Discovery Server Access
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8180/eureka/
#Puerto dinamico
server:
  port: ${PORT:${SERVER_PORT:0}}
